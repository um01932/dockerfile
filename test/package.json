{
  "name": "@myworkplace/api",
  "version": "0.3.2",
  "description": "Abstraction layer for communication between browsing contexts based on message channels",
  "scripts": {
    "build": "rimraf ./lib && rimraf ./doc/generated && typedoc --out doc/generated --mode file && webpack && node config/createDelivery.js"
  },
  "files": [
    "lib/*"
  ],
  "keywords": [
    "MessageChannel",
    "iframe",
    "promises",
    "myWorkplace"
  ],
  "main": "lib/workplace-api.js",
  "types": "lib/Messaging.d.ts",
  "author": "Tobias.Straller@bmw.de",
  "devDependencies": {
    "archiver": "^3.0.0",
    "copyfiles": "^1.2.0",
    "rimraf": "^2.6.1",
    "ts-loader": "^5.3.3",
    "typedoc": "^0.14.2",
    "typescript": "^3.2.4",
    "webpack": "^4.29.0",
    "webpack-cli": "^3.2.1",
    "es6-promise": "^4.2.5"
  }
}
